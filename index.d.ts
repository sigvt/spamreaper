export declare function spamScore(text: string): number;

export declare function concatinatedScore(input: string[]): number;

export declare function isSpam(inputs: string[], threshold?: number): boolean;
